import { notFound } from "next/navigation"; import { findStory, loadSourceSite } from "@/lib/adapter/mappers";
export default function StoryPage({params}:{params:{id:string}}){const item=findStory(params.id); if(!item) notFound(); const s=loadSourceSite(); return <main className="mx-auto max-w-4xl px-6 py-10"><p className="text-sm text-neutral-500">{s.site.name} · {item.topic}</p><h1 className="mt-2 text-3xl font-semibold">{item.title}</h1><p className="mt-2 text-sm text-neutral-600">{item.published_at}</p><a className="mt-6 inline-block underline" href={item.link} target="_blank" rel="noreferrer">Abrir fuente</a></main>}
