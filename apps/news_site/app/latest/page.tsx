import Link from "next/link"; import { loadSourceSite } from "@/lib/adapter/mappers";
export default function LatestPage(){const s=loadSourceSite(); return <main className="mx-auto max-w-4xl px-6 py-10"><h1 className="text-3xl font-semibold">{s.site.name}</h1><p className="mt-2 text-neutral-600">{s.site.tagline}</p><div className="mt-8 space-y-6">{s.latest.map((item:any)=><article key={item.index_id} className="border-b pb-4"><div className="text-xs text-neutral-500">{item.source}</div><Link href={`/story/${item.index_id}`} className="mt-1 block text-xl font-medium">{item.title}</Link><div className="mt-1 text-sm text-neutral-600">{item.published_at}</div></article>)}</div></main>}
