import Link from "next/link";
import { loadSourceSite } from "@/lib/adapter/mappers";
export default function HomePage() {
 const snapshot=loadSourceSite(); const {site,hero,latest,sections}=snapshot;
 return <main className="mx-auto max-w-6xl px-6 py-10"><header className="border-b pb-6"><div className="text-xs uppercase tracking-[0.2em] text-neutral-500">{site.locale}</div><h1 className="mt-2 text-4xl font-semibold">{site.name}</h1><p className="mt-2 text-neutral-600">{site.tagline}</p></header><section className="mt-8 border-b pb-8"><div className="text-sm text-neutral-500">{hero.source}</div><h2 className="mt-2 text-3xl font-semibold">{hero.title}</h2><p className="mt-2 text-sm text-neutral-600">{hero.published_at}</p><Link href={`/story/${hero.index_id}`} className="mt-4 inline-block text-sm underline">Ver noticia</Link></section><section className="mt-10 grid gap-8 lg:grid-cols-[2fr,1fr]"><div><div className="mb-4 flex justify-between"><h3 className="text-xl font-semibold">Últimas noticias</h3><Link href="/latest" className="text-sm underline">Ver todo</Link></div>{latest.map((item:any)=><article key={item.index_id} className="border-b py-4"><div className="text-xs text-neutral-500">{item.source}</div><Link href={`/story/${item.index_id}`} className="mt-1 block text-lg font-medium">{item.title}</Link></article>)}</div><aside><h3 className="mb-4 text-xl font-semibold">Secciones</h3>{sections.map((section:any)=><Link key={section.topic} href={`/topic/${encodeURIComponent(section.topic)}`} className="mb-3 block border p-4">{section.topic} · {section.article_count}</Link>)}</aside></section></main>;
}
