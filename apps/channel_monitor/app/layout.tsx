import "./globals.css";
export const metadata = { title: "Channel Monitor", description: "Governed recent-activity monitor for configured public video channels." };
export default function RootLayout({ children }: { children: React.ReactNode }) { return <html lang="en"><body>{children}</body></html>; }
