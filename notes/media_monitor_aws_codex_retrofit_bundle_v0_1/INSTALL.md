# Installation note

Copy the `docs/` directory from this bundle into the repository root.

Then tell Codex:

> Read `docs/retrofit/aws_media_sensing/CODEX_START_HERE.md` and execute only `PR-A0`. Update its closure note and propose the carry-state transition. Stop when the PR is ready for review.
